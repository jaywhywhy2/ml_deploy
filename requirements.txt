streamlit
scikit-learn==1.3.0
pandas
numpy
pickle5
```

**주의사항:**
- scikit-learn의 경우 버전이 중요합니다. 모델을 학습할 때 사용한 버전과 동일하게 지정하세요
- 다른 라이브러리(XGBoost, LightGBM, TensorFlow 등)를 사용했다면 모두 포함해야 합니다

**2. Python 버전 확인**

`.streamlit/config.toml` 또는 저장소 루트에 `.python-version` 파일을 만들어 Python 버전을 명시하세요:
```
3.9
```

또는 `runtime.txt` 파일:
```
python-3.9